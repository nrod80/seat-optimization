// currentSeats is an array of strings whose index corresponds
// to their current seat
// for example = ['NickR', 'Doug', 'ChrisC'...]
// NickR is in seat 0, Doug is in seat 1, ChrisC is in seat 3...

// pairs is an object mimicking pairs issued from slack
// { Connie: 'Yi',
//   Yi: 'Connie',
//   Peter: 'Ryan',
//   Ryan: 'Peter',
//   ...}

// export a function that returns an array of the final seating chart

module.exports = function(currentSeats, pairs){

	return finalPlacementArray;
}
